package chapter_13;

public class Meimo {
	//Fileクラスに読み込むファイルを指定する
	/*String path = ("C:\\Users\\user\\Desktop\\tian\\mensetu.txt");
	
	DefaultTableModel df = (DefaultTableModel) jTable1.getModel();
	int row = jTable1.getSelectedRow();
	
	try (FileInputStream fs = new FileInputStream(path);
	        InputStreamReader fr = new InputStreamReader(fs, "Shift-JIS");
	        BufferedReader br = new BufferedReader(fr)) {
	    
	    String line;
	    while ((line = br.readLine()) != null) {
	        String[] temp =line.split(",");
	        df.addRow(temp);
	        
	    }
	    
	} catch (FileNotFoundException e) {
	    e.printStackTrace();
	} catch (IOException e) {
	    e.printStackTrace();
	}
	
	
	}                                        
	*/

	/*int column = 0;
	int row = jTable1.getSelectedRow();
	String xxxx = jTable1.getValueAt(row, column).toString();
	*/

	/*
	 * List<String> userInfoList = new ArraysList<String>();
	
	// 表 取到的ID
	String userId = "003";
	
	// 读取txt
	
	String[] userInfo = line.split(",");
	if(userId.equals(userInfo[0])){
	continue;
	
	}
	
	userInfoList.add(line);
	 */
	
	/*
	 * try {
            FileWriter fileWriter = new FileWriter("C:\\Users\\user\\Desktop\\tian\\mensetu.txt");
            fileWriter.write(content);
            fileWriter.close();
           
        } catch (IOException e) {
  
            e.printStackTrace();
        }
    }
}
	 */
	
	/*清空
	 * df.setRowCount(0);
	 */
}
